# Mini File Concatenator

一个轻量级的 GUI 工具，用于递归遍历目录并拼接所有文本文件的内容。

## 功能

- 递归遍历指定目录
- 只处理文本文件（自动跳过图片、PDF 等二进制文件）
- 按文件名排序输出
- 显示文件路径作为分隔标记
- 进度条显示处理进度
- 后台线程执行，界面不卡顿

## 支持的文件类型

.txt, .py, .java, .c, .cpp, .h, .js, .html, .css, .json, .yaml, .md, .log, .csv 等
